# Database
DB_ROOT_PASSWORD=rootpassword
DB_NAME=loaners
DB_USER=loaners
DB_PASSWORD=loanerspass
DB_HOST=localhost
DB_PORT=3306

# JWT
JWT_SECRET=change_me_to_a_long_random_secret_in_production

# Email (optional, for overdue notifications)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=notifications@school.edu
SMTP_PASS=your_smtp_password
EMAIL_FROM=ICT Department <ict@school.edu>

# Server
PORT=3000
NODE_ENV=development
